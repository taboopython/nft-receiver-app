version: "3.8"
services:
  nft-receiver-app:
    container_name: nft-receiver-app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
    command: ["npm", "start"]
